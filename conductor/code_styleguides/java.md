# Java Code Style Guide

- Follow standard Java naming conventions (CamelCase for classes, camelCase for methods and variables).
- Prioritize Domain Driven Design principles (clear module boundaries).
- Keep classes small and single-responsibility.
- Use explicit types when generics are deeply nested, otherwise `var` is acceptable for local variables (if Java 10+).
- Leverage modern Java features (records, switch expressions) where appropriate.