{
    "general": {
        "enableNotifications": true
    },
    "hooks": {
        "Notification": [
            {
                "matcher": "*",
                "hooks": [
                    {
                        "name": "intellij-terminal-watcher",
                        "type": "command",
                        "command": "bash -c '[ \"$TERMINAL_EMULATOR\" != \"JetBrains-JediTerm\" ] && exit 0; INPUT=$(cat); P=$$; while [ \"$P\" != \"1\" ]; do P=$(ps -o ppid= -p \"$P\" 2>/dev/null | xargs); [ -z \"$P\" ] && exit 0; [ -f ~/.terminal-watcher/ports/\"$P\".port ] && { PORT=$(cat ~/.terminal-watcher/ports/\"$P\".port); echo \"$INPUT\" | curl -s -X POST \"http://127.0.0.1:$PORT/event?tool=gemini\" -H \"Content-Type: application/json\" -d @-; exit 0; }; done'",
                        "timeout": 5000
                    }
                ]
            }
        ],
        "AfterAgent": [
            {
                "matcher": "*",
                "hooks": [
                    {
                        "name": "intellij-terminal-watcher",
                        "type": "command",
                        "command": "bash -c '[ \"$TERMINAL_EMULATOR\" != \"JetBrains-JediTerm\" ] && exit 0; INPUT=$(cat); P=$$; while [ \"$P\" != \"1\" ]; do P=$(ps -o ppid= -p \"$P\" 2>/dev/null | xargs); [ -z \"$P\" ] && exit 0; [ -f ~/.terminal-watcher/ports/\"$P\".port ] && { PORT=$(cat ~/.terminal-watcher/ports/\"$P\".port); echo \"$INPUT\" | curl -s -X POST \"http://127.0.0.1:$PORT/event?tool=gemini\" -H \"Content-Type: application/json\" -d @-; exit 0; }; done'",
                        "timeout": 5000
                    }
                ]
            }
        ]
    }
}